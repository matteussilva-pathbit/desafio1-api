using Microsoft.AspNetCore.Identity;

namespace Infrastructure.Identity;

public class AppUser : IdentityUser
{
    // Você pode adicionar propriedades extras se quiser
    // public string NomeCompleto { get; set; } = string.Empty;
}
